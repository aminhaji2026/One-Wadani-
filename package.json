{
  "name": "waddani-party-platform",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "sh -c 'npm --workspace apps/api run dev & npm --workspace apps/web run dev; wait'",
    "dev:api": "npm --workspace apps/api run dev",
    "dev:web": "npm --workspace apps/web run dev",
    "build": "npm --workspace apps/api run build && npm --workspace apps/web run build",
    "db:generate": "prisma generate --schema prisma/schema.prisma",
    "db:migrate": "prisma migrate dev --schema prisma/schema.prisma",
    "db:deploy": "prisma migrate deploy --schema prisma/schema.prisma",
    "db:seed": "tsx scripts/seed.ts",
    "typecheck": "npm --workspace apps/api run build --if-present && npm --workspace apps/web exec tsc -b --pretty false"
  },
  "devDependencies": {
    "prisma": "^6.3.1",
    "tsx": "^4.19.2"
  },
  "dependencies": {
    "@prisma/client": "^6.3.1",
    "bcryptjs": "^2.4.3"
  }
}
